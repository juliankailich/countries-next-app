import { PlaceholdersConfig } from "./model/placeholders/placeholders.model";

export const SEARCH_TYPE_PLACEHOLDERS: PlaceholdersConfig = {
  name: "Search by country name",
  code: "Search by ISO code",
  continent: "Search by continent CODE (ex. South America: SA)",
};
